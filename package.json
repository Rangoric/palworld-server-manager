{
  "name": "palworld-server-manager-electron",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && electron .",
    "dev": "tsc -p . --watch",
    "package": "echo Packaging not configured"
  },
  "devDependencies": {
    "electron": "^26.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
